{
  // List of modules to load at startup
  initModules : [
    'helloworld',
    'foo',
  ],

  // Or we can load all modules
  // If true, overrides initModules
  initAllModules : false,

  // Applies to init and the command interperter in general
  // Can be overriden via cli
  // die, yell, continue
  errorAction: 'die',
}
